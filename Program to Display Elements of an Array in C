#include <stdio.h>

int main(void)
{
    int arr[] = {45, 78, 90, 76, 60, 34};
    int size = sizeof(arr) / sizeof(arr[0]);

    printf("Array elements are:\n");

    for (int i = 0; i < size; i++)
    {
        printf("%d ", arr[i]);
    }

    return 0;
}
